{% extends 'base.html.twig' %}

{% block title %} My favoris - my_Burger{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{asset('assets/css/product/card_product.css')}}" type="text/css">
            
{% endblock %}

{% block body %}    
    <div class="content_favoris">
        {% for favori in allFavoris %}
        
            <div class="card" style="width: 18rem;">
                    <div class="img_card">
                        <img src="/uploads/images/products/{{favori.illustration}}" class="card-img-top" alt="{{favori.name}}">
                        <a href="#!" title="ajouter aux favoris"><i class="far fa-heart"></i></a>
                        <a href="{{path('add_favoris', {'userId': app.user.id , 'productId' : favori.id} )}}" title="ajouter aux favoris"><i , class="fas fa-heart"></i></a>
                    </div>
                    <div class="card-body">
                        <h5 class="card-title">{{favori.name}}</h5>
                        <a href="#!" class="food-card_author">{{favori.category.getName()}}</a>
                        <p class="card-text">{{favori.description}}</p>
                        <hr/>
                        <div class="space-between">
                            <div class="food-card_price">
                                <span class=""> {{favori.prix}} €</span>
                            </div>
                            <div class="food-card_order-count">
                                <div class="input-group mb-3">
                                    <div class="input-group-prepend">
                                        <button class="btn btn-outline-secondary minus-btn" type="button" id="button-addon1"><i class="fa fa-minus"></i></button>
                                    </div>
                                    <input type="text" class="form-control input-manulator" placeholder="" aria-label="Example text with button addon" aria-describedby="button-addon1" value="0">
                                    <div class="input-group-append">
                                        <button class="btn btn-outline-secondary add-btn" type="button" id="button-addon1"><i class="fa fa-plus"></i></button>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="none">
                            <span class="a"> {{favori.id}}</span>
                            <span class="b"></span>
                            {% if favori %}
                                <span class="c">1</sapn>
                            {% endif %}
                        </div>
                        <div class="d-grid gap-2">
                            <a href="" class="btn btn-info">Ajouter au panier</a>
                        </div>
                    </div>
                </div>
        {% endfor %}
    </div>
   
{% endblock %}
{% block javascripts %}
        <script src="{{asset('assets/js/addProduct/ajoutProduit.js')}}" type="text/javascript"></script> 
        <script src="{{asset('assets/js/addFavoris/Favoris.js')}}" type="text/javascript"></script>       
{% endblock %}