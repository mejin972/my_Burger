{% extends 'base.html.twig' %}

{% block title %}Admin - my_Burger {% endblock %}

{% block body %}
    <div class="content_category">
    
    <a href="{{path('admin_editRang')}}" class="btn btn-primary"> Edit </a> <br />
    <a href="{{path('admin')}}" class="btn btn-primary"> Retour </a>

        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th scope="col"></th>
                    <th scope="col">Name</th>
                    <th scope="col">Condition d'obtention</th>
                    <th scope="col">Nombre d'utilisateur</th>
                </tr>
            </thead>
            <tbody>
   
                {% for nbUser in completeRang %}
                        <tr>
                            <th scope="row"></th>
                            <td>{{nbUser.name}}</td>
                            <td>{{nbUser.conditionObtention}}</td>
                            <td>{{nbUser.nbUser}}</td>
                        </tr>
                {% endfor %}
                
            </tbody>
        </table>
    </div>
{% endblock %}
