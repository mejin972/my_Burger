{% extends 'base.html.twig' %}

{% block title %}Admin - my_Burger {% endblock %}

{% block body %}
    <div class="content_admin_products">
    <a href="{{path('admin')}}" class='btn btn-primary'> Retour </a><br />
    <a href="{{path('admin_editProduct')}}" class='btn btn-primary'> Edit </a>
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th scope="col">Illustration</th>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                    <th scope="col">Prix</th>
                </tr>
            </thead>
            <tbody>
                {% for product in allProducts %}
                    <tr>
                        <th scope="row"><img src="/uploads/images/products/{{product.illustration}}" alt="..." class='img_product'></th>
                        <td>{{product.name}}</td>
                        <td>{{product.description}}</td>
                        <td>{{product.prix}} €</td>
                        <td> <a href="{{path('admin_updateProduct' , {'slug' : product.slug} )}}"> Modifier </a> </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
